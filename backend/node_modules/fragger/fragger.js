// Christian Egon Sørensen©, MIT license
const readlineSync = require('readline-sync');

exports.fragedShower function(a,al) {
	// Takes argument a = array
	// And al = array.length
	let dA = [] // The superarray [2D]

	let tA = [] //Temporary array

	for(let i = 0; i < al; i++){
		if((i%f === 0 && i != 0) || (i+1 >= al)){
			dA.push(tA); //Push tempArray to superarray
			tA = []; //Flush tempArray
		}
		else {
			tA.push(a[i]);
		}
	}
	// The actual showing of the fragged elements
	for(let i = 0; i < dA.length; i++) {
		readlineSync.keyInSelect(dA[i]);
	}
}
